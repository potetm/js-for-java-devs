var manyArgs = function(args) {
  return args.i + args.have + args.too + args.many + args.args + args.to + args.handle;
};

manyArgs({
  i: 'I',
  have: 'have',
  too: 'too',
  many: 'many',
  args: 'args',
  to: 'to',
  handle: 'handle'
});
